# Core dependencies
apache-airflow==2.6.0

# Kafka integration
kafka-python==2.0.2

# HTTP requests for API calls
requests==2.31.0

# Optional but recommended for better performance
urllib3==2.0.7
certifi==2024.2.2

# Airflow providers (اختياري للتوثيق بشكل أفضل)
apache-airflow-providers-apache-kafka==1.3.0